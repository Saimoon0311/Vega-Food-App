import React from 'react';
import {View, Text} from 'react-native';

const HoemScreen = ({navigation}) => {
  return (
    <View>
      <Text>kajssbfkjasb</Text>
    </View>
  );
};

export default React.memo(HoemScreen);
